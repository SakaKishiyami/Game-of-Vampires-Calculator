export default function AccountPage() {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-semibold text-white">Accounts</h1>
      <p className="text-sm text-gray-300 max-w-2xl">
        Dedicated space for account management. We can move or mirror the existing Discord / Supabase
        login UI here when you&apos;re ready, while still keeping quick access from inside the main
        calculator if you want.
      </p>
    </div>
  )
}

